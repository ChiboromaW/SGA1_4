# lets create a list
# break statement
stationaries = ['pencil','eraseer','ruler']
# exit the loop when it gets to eraser
for x in stationaries:
    if x == 'eraser':
        break
print ('current x:', x)